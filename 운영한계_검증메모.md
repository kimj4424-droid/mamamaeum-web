# 운영 한계와 출시 전 재검증 메모

작성일: 2026-09-07

## 현재 확인된 상태
- Vercel 프로덕션에서 입력 형식·길이 오류(400/413), 요청 제한(429), `Cache-Control: no-store`, AI 실제 호출(200)을 확인했다.
- 피드백 API는 빈 입력을 저장하지 않고 거절하며, 배포 환경변수 설정이 준비돼 있음을 확인했다.

## 남은 운영 한계
1. 요청 제한은 Vercel 함수 인스턴스별 메모리 방식이다. 여러 인스턴스 전체의 요청량을 합산하지 못한다.
2. Anthropic 네트워크 단절에 따른 502 응답, Google Forms 실제 저장 성공은 운영 데이터에 영향을 주지 않기 위해 아직 재현하지 않았다.
3. Vercel API에는 momslator의 실서비스·QR 테스트 Origin만 허용하는 CORS 정책을 적용했다. 토스 QR 샌드박스 실기기 호출은 아직 확인해야 한다.

## 출시 전 할 일
- Vercel Firewall 또는 Redis 기반 전역 요청 제한과 오류 알림을 설정한다.
- 테스트 전용 Google Form으로 피드백 성공·실패·삭제 요청 흐름을 검증한다.
- 토스 샌드박스에서 클립보드 읽기/쓰기 권한 거부·허용과 뒤로가기 동작을 확인한다.
- `apps-in-toss.config.ts`의 `appName`이 토스 콘솔의 실제 appName과 정확히 일치하는지 확인한다.
